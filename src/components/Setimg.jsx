import React from 'react'

function Setimg({props}) {
 
    const {data} = props


    console.log(data);
    

  return (
    <>
        {/* {props ? <div></div> : <img src={props.main} />} */}

    </>
  )
}

export default Setimg